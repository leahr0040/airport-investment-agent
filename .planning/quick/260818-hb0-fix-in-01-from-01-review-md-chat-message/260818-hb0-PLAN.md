---
phase: quick
plan: 260818-hb0
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
requirements: [IN-01]
must_haves:
  truths:
    - "Each ChatMessage rendered in the list has a stable id independent of its array position"
    - "React key for the message list is message.id, not the array index"
  artifacts:
    - src/app/page.tsx
  key_links:
    - "ChatMessage.id populated at all three construction sites (user submit, agent/error response, catch-block error) before being pushed into state"
---

<objective>
Fix IN-01 from `01-REVIEW.md`: `src/app/page.tsx` keys the rendered chat message list on array index (`key={i}`). Add a stable `id` field to `ChatMessage` and key on it instead, closing the latent React key bug before any delete/edit/reorder behavior is added.

Purpose: Remove a documented review finding (IN-01) — not a live bug today (list is append-only) but a footgun for future list mutation.
Output: `src/app/page.tsx` updated; `ChatMessage` has a required `id: string`; all three message-construction sites populate it; render key uses `message.id`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-configuration-airport-registry-resolution/01-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add stable id to ChatMessage and key the render list on it</name>
  <files>src/app/page.tsx</files>
  <action>
Per IN-01 (01-REVIEW.md): add `id: string` to the `ChatMessage` type (line 7), making it `{ id: string; role: 'user' | 'agent' | 'error'; text: string }`.

Generate `id: crypto.randomUUID()` at each of the three places a `ChatMessage` object literal is constructed and pushed into `messages` state via `setMessages`:
1. The user message on submit (currently `{ role: 'user', text: query }`, around line 20).
2. The agent/error response after fetch, inside the ternary (currently `body.ok ? { role: 'agent', text: body.data.narrative } : { role: 'error', text: body.error.message }`, around line 34) — generate one `crypto.randomUUID()` value and reuse it for whichever branch of the ternary resolves, so both branches get a fresh id from the same call site.
3. The error message in the catch block (currently `{ role: 'error', text: 'Could not reach the server. Please try again.' }`, around line 37).

Change the render key in the `.map()` call (currently `messages.map((message, i) => (<div key={i} ...>`, around line 54) from `key={i}` to `key={message.id}`, and drop the now-unused `i` parameter from the map callback signature.

No other behavior change: role/text values, conditional styling, loading state, and the fetch/catch flow stay exactly as they are.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>`ChatMessage` has a required `id: string` field; all three construction sites populate it with `crypto.randomUUID()`; the message list renders with `key={message.id}`; `npm run typecheck` and `npm test` both pass with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed — this is a client-side rendering key fix with no new external input, no new I/O, and no change to what is sent to or received from the server.

## STRIDE Threat Register

No new threats introduced. `crypto.randomUUID()` is the standard Web Crypto API, already in use elsewhere in this file for `sessionId` (line 10).
</threat_model>

<verification>
1. `npm run typecheck` — zero TypeScript errors (confirms `ChatMessage.id` is required and satisfied at all three construction sites, and `message.id` resolves in the `.map()` key).
2. `npm test` — full Vitest suite passes (confirms no existing test constructs a `ChatMessage` literal that is now missing the required `id` field; grep already confirmed no such test exists, this is the safety net).
3. Manual/visual: not required — no rendered-output change, purely a key-stability fix with identical DOM structure and styling.
</verification>

<success_criteria>
- `src/app/page.tsx`'s `ChatMessage` type includes `id: string`.
- All three message-construction call sites assign `id: crypto.randomUUID()`.
- The message list's `.map()` renders `key={message.id}` instead of `key={i}`.
- `npm run typecheck` and `npm test` both exit 0.
- Change committed.
</success_criteria>

<output>
Create `.planning/quick/260818-hb0-fix-in-01-from-01-review-md-chat-message/260818-hb0-SUMMARY.md` when done.
</output>
