---
phase: 01-foundation-configuration-airport-registry-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - vitest.config.ts
  - .gitignore
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/domain/airports/types.ts
autonomous: false
requirements: [SETUP-02]

user_setup: []

must_haves:
  truths:
    - "`npm install` on a fresh clone succeeds and `npx tsc --noEmit` reports zero errors"
    - "`npm run dev` starts a Next.js 16 server on http://localhost:3000 that returns HTTP 200"
    - "`npm test` invokes Vitest and Vitest resolves the `@/` path alias to `src/`"
    - "`.gitignore` prevents `.env` / `.env.local` from being committed while still tracking `.env.example`"
    - "Every downstream module can import the airport domain contracts from one file without inventing its own shapes"
  artifacts:
    - path: "package.json"
      provides: "Next.js 16 + TypeScript app manifest with dev/build/lint/test scripts and zod, server-only, vitest deps"
      contains: "\"test\""
    - path: "vitest.config.ts"
      provides: "Vitest node-environment config with @/ -> src alias"
      contains: "alias"
    - path: ".gitignore"
      provides: "Secret-file exclusion with an explicit .env.example negation"
      contains: "!.env.example"
    - path: "src/domain/airports/types.ts"
      provides: "Shared airport domain contracts consumed by plans 01-03 and 01-04"
      exports: ["AirportRef", "Registry", "ResolveResult", "RunwaySummary", "ParallelGroup", "RegionName", "MatchKind"]
      min_lines: 45
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json"
      via: "the @/* import alias must resolve identically in Vitest and in the Next.js build"
      pattern: "alias"
    - from: "src/domain/airports/types.ts"
      to: "src/domain/airports/*"
      via: "every later airport module imports its contracts from types.ts rather than redeclaring them"
      pattern: "from ['\"]\\./types['\"]"
---

<objective>
Turn the empty repository into a runnable, type-checked, test-capable Next.js 16 + TypeScript
application, and publish the shared airport domain contracts that plans 01-03 and 01-04 build
against in parallel.

Purpose: SETUP-02 requires a reviewer to go from `git clone` to a running app. That is impossible
until a real Next.js app exists. This plan is also the interface-first step: by defining
`AirportRef` / `Registry` / `ResolveResult` up front, the resolver plan and the registry-fetch
plan can be written against the same contracts without either waiting on the other.

Output: A booting Next.js app, a working Vitest toolchain, a secret-safe `.gitignore`, and
`src/domain/airports/types.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-configuration-airport-registry-resolution/01-CONTEXT.md
@.planning/phases/01-foundation-configuration-airport-registry-resolution/01-RESEARCH.md
</context>

## Artifacts this phase produces

Symbols and files created by Phase 1 (all are new — none exist in the repository today).
Plan 01-01 creates the entries marked **[01-01]**; the rest are listed so reviewers do not
flag them as unresolved references.

| Artifact | Kind | Created by |
|----------|------|------------|
| `package.json`, `package-lock.json`, `tsconfig.json`, `next.config.ts` | Next.js scaffold files | **[01-01]** |
| `vitest.config.ts` | Test runner config | **[01-01]** |
| `.gitignore` | Repo config | **[01-01]** |
| `src/app/layout.tsx`, `src/app/page.tsx` | Next.js App Router entry | **[01-01]** |
| `src/domain/airports/types.ts` | Module | **[01-01]** |
| `RunwaySummary`, `ParallelGroup`, `AirportRef`, `Registry`, `RegionName`, `MatchKind`, `ResolveResult` | Exported types | **[01-01]** |
| `src/config/env.schema.ts` — `EnvSchema`, `Env`, `parseEnv`, `EnvValidationError`, `REQUIRED_ENV_HELP` | Module + symbols | 01-02 |
| `src/config/env.ts` — `env`, `getEnv` | Module + symbols | 01-02 |
| `src/instrumentation.ts` — `register` | Next.js boot hook | 01-02 (extended by 01-04) |
| `.env.example`, `README.md` (rewritten) | Reviewer runbook | 01-02 |
| `src/domain/airports/regions.ts` — `STATE_TO_REGION`, `STATE_NAME_TO_CODE`, `REGION_ALIASES`, `airportsInRegion`, `airportsInState` | Module + symbols | 01-03 |
| `src/domain/airports/metroClusters.ts` — `METRO_CLUSTERS`, `METRO_ALIASES` | Module + symbols | 01-03 |
| `src/domain/airports/aliases.ts` — `LEGACY_CODE_ALIASES`, `LEGACY_CODE_NOTES` | Module + symbols | 01-03 |
| `src/domain/airports/resolve.ts` — `resolve`, `normalizeQuery` | Module + symbols | 01-03 |
| `src/domain/airports/allowlist.ts` — `AIRPORT_CODE_PATTERN`, `UnknownAirportError`, `isAllowedAirportCode`, `assertAllowedAirport`, `airportCodeSchema` | Module + symbols | 01-03 |
| `src/domain/airports/fetchArcGis.ts` — `ARCGIS_BASE`, `queryAllFeatures`, `ArcGisQueryError` | Module + symbols | 01-04 |
| `src/domain/airports/geometry.ts` — `bearingDeg`, `toLocalMeters`, `headingDeltaDeg`, `PARALLEL_HEADING_TOLERANCE_DEG`, `deriveParallelGroups` | Module + symbols | 01-04 |
| `src/domain/airports/registry.ts` — `FACILITY_WHERE`, `FACILITY_FIELDS`, `RUNWAY_WHERE`, `RUNWAY_FIELDS`, `buildRegistry`, `initRegistry`, `getRegistry` | Module + symbols | 01-04 |
| `src/domain/airports/fixtures/testRegistry.ts` — `makeTestRegistry` | Test fixture builder | 01-03 |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate before any npm install</name>
  <files>package.json (not yet created — this gate runs before it exists)</files>
  <read_first>
    - `.planning/phases/01-foundation-configuration-airport-registry-resolution/01-RESEARCH.md` — the "Package Legitimacy Audit" table and its SUS note on `server-only`
  </read_first>
  <action>
    Do not run `npm install` or `create-next-app` before this gate is approved. Run the six
    verification commands listed under how-to-verify, present their output to the developer, and
    wait for an explicit approval. This checkpoint is never auto-approvable.
  </action>
  <acceptance_criteria>
    - `npm view server-only scripts` output contains no `postinstall` or `preinstall` entry.
    - `npm view vitest repository.url` resolves to the `vitest-dev/vitest` GitHub repository.
    - `npm view vitest scripts` output contains no `postinstall` or `preinstall` entry.
    - The generator invoked in Task 2 is named exactly `create-next-app` and is version-pinned to `@16.3.0`.
    - The developer has typed an explicit approval before any install command runs.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const{execSync}=require('child_process');for(const p of ['server-only','vitest']){const s=execSync('npm view '+p+' scripts --json',{encoding:'utf8'}).trim();if(/postinstall|preinstall/.test(s))throw new Error(p+' declares an install script: '+s)}const r=execSync('npm view vitest repository.url',{encoding:'utf8'});if(!/vitest-dev\/vitest/.test(r))throw new Error('unexpected vitest repository: '+r);console.log('package legitimacy signals OK — awaiting human approval')"</automated>
  </verify>
  <done>The developer has reviewed the maintainer, install-script and repository signals for `server-only` and `vitest`, confirmed the generator name is not a typosquat, and explicitly approved proceeding to install.</done>
  <what-built>
    Nothing yet — this gate runs BEFORE the first `npm install`, per the package legitimacy
    protocol. RESEARCH.md's Package Legitimacy Audit covers `zod` (verdict OK) and
    `server-only` (verdict SUS, flagged only for a missing `repository` field). Two further
    packages are being introduced at planning time and are therefore unaudited / ASSUMED:
    `vitest` (test runner, chosen by the planner — see the decision note in Task 2) and the
    package set that `create-next-app` installs (`next`, `react`, `react-dom`, `typescript`,
    `@types/*`, `eslint`, `eslint-config-next`, `tailwindcss`, `@tailwindcss/postcss`).
  </what-built>
  <how-to-verify>
    Run each command and confirm the result before approving. Do not run `npm install` first.

    1. `npm view server-only` — confirm: version 0.0.1, maintained under the `vercel` /
       Next.js umbrella, weekly downloads in the millions, and no `postinstall` script.
       Cross-check that the package is documented on nextjs.org (search "server-only" in the
       Next.js docs). Expected conclusion: SUS flag is a false positive caused solely by the
       absent `repository` field.
    2. `npm view server-only scripts` — expected: no `postinstall` / `preinstall` entry.
    3. `npm view vitest repository.url` — expected: `git+https://github.com/vitest-dev/vitest.git`.
    4. `npm view vitest scripts` — expected: no `postinstall` / `preinstall` entry.
    5. Open https://www.npmjs.com/package/vitest and https://www.npmjs.com/package/server-only
       and confirm both pages show the expected maintainers and a plausible download history.
    6. Confirm the scaffold command in Task 2 uses the official generator name
       `create-next-app` pinned to `@16.3.0` — not a look-alike such as `create-nextjs-app`
       or `create-next-app-ts`.

    If any check fails (unexpected maintainer, an install script, a typosquat name), STOP and
    report rather than approving.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to installs, or describe what looked wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js 16 + TypeScript, install dependencies, wire up Vitest</name>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, vitest.config.ts, .gitignore, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    - `.planning/phases/01-foundation-configuration-airport-registry-resolution/01-RESEARCH.md` — "Standard Stack", "Recommended Project Structure", "Environment Availability"
    - `.planning/PROJECT.md` — Constraints (Next.js + TypeScript, single repo) and the Key Decisions table
    - `.claude/CLAUDE.md` — "Q3 — Supporting TypeScript Libraries" (the What NOT to install table) and the Shell/Environment rules for this Windows machine
    - `README.md` — the current repo root README, which must survive the scaffold untouched in this task
  </read_first>
  <action>
    Scaffold a Next.js 16.3.0 App Router + TypeScript application into the existing repository
    root. `create-next-app` refuses to write into a directory containing unrecognised entries
    (this repo already has `.claude/` and `.planning/`), so scaffold into a throwaway
    subdirectory and move the output up.

    Step 1 — generate. Run, from the repo root:
    `npx --yes create-next-app@16.3.0 .next-scaffold --ts --app --src-dir --eslint --tailwind --import-alias "@/*" --use-npm --empty --skip-install --disable-git --yes`
    Pass every option explicitly (as above) so any saved `create-next-app` preferences on this
    machine cannot change the result. If the CLI rejects one flag as unknown, drop only that
    flag and rerun; never fall back to the interactive prompt flow.

    Step 2 — relocate. Delete `.next-scaffold/README.md` and `.next-scaffold/AGENTS.md` if
    present (the repo root `README.md` is authoritative and is rewritten by plan 01-02). Move
    `.next-scaffold/.gitignore` to the repo root, then move the remaining visible entries with
    `mv .next-scaffold/* ./`, then run `ls -a .next-scaffold` and move any leftover dotfiles
    individually before removing the directory. Confirm the repo root now holds
    `package.json`, `tsconfig.json`, `next.config.ts`, `next-env.d.ts`, `eslint.config.mjs`,
    `postcss.config.mjs`, `.gitignore`, `public/`, and `src/app/` with `layout.tsx`,
    `page.tsx`, `globals.css`.

    Step 3 — secret-safe `.gitignore`. The generated `.gitignore` ignores `.env*`, which would
    also swallow the `.env.example` that SETUP-02 depends on. Append a negation line
    `!.env.example` after the env block. Also confirm `node_modules`, `.next`, and
    `*.tsbuildinfo` are ignored.

    Step 4 — dependencies. Run `npm install` to materialise the scaffold's own dependencies,
    then `npm install zod@4.4.3 server-only@0.0.1` and `npm install -D vitest@4.1.10`. Do not
    upgrade or pin `typescript` beyond whatever `create-next-app` selected — TypeScript 7.x is
    published but is not the version this Next.js release is validated against.

    Planner decision to record in the plan summary: Vitest is the test runner for this project.
    RESEARCH.md names `env.test.ts` / `resolve.test.ts` but never picks a runner, and SCORE-05
    in a later phase makes `npm test` a graded deliverable. Vitest is one dev dependency, needs
    no Babel or ts-jest bridge, runs TypeScript and ESM natively, and resolves the `@/*` alias
    from a four-line config. Node's built-in `node:test` was considered and rejected because it
    cannot resolve the `@/*` alias without a custom loader.

    Step 5 — Vitest config. Create `vitest.config.ts` at the repo root exporting
    `defineConfig` from `vitest/config` with `test.environment` set to `'node'`,
    `test.include` covering `src/**/*.test.ts`, `test.testTimeout` of 30000 (the registry
    integration test in plan 01-04 hits a live FAA endpoint), and `resolve.alias` mapping
    `'@'` to the absolute path of `src` computed from `import.meta.url` via
    `fileURLToPath` + `path.dirname` (do not use `__dirname`; this config is ESM).

    Step 6 — scripts. Ensure `package.json` `scripts` contains `dev` (`next dev`), `build`
    (`next build`), `start` (`next start`), `lint` (`next lint` or `eslint .`, whichever the
    scaffold produced), `typecheck` (`tsc --noEmit`), `test` (`vitest run`), and `test:watch`
    (`vitest`).

    Step 7 — directory. Create the empty directory `src/domain/airports/` so Task 3 and the
    later plans have their target location.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --passWithNoTests && node -e "const p=require('./package.json');const s=p.scripts||{};for(const k of ['dev','build','start','test','typecheck']){if(!s[k])throw new Error('missing script: '+k)};for(const d of ['next','react','zod','server-only']){if(!(p.dependencies||{})[d])throw new Error('missing dep: '+d)};if(!(p.devDependencies||{}).vitest)throw new Error('missing devDep: vitest');console.log('package.json OK')" && grep -q '^!\.env\.example$' .gitignore && echo 'gitignore OK' && test ! -d .next-scaffold && echo 'scaffold dir removed'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run --passWithNoTests` exits 0, proving the Vitest binary and config load.
    - `package.json` `dependencies` contains `next`, `react`, `react-dom`, `zod`, `server-only`; `devDependencies` contains `vitest` and `typescript`.
    - `package.json` `scripts` contains all of: `dev`, `build`, `start`, `lint`, `typecheck`, `test`, `test:watch`; `scripts.test` is exactly `vitest run`.
    - `.gitignore` contains a line that is exactly `!.env.example`, positioned after the line matching `.env*`.
    - `git check-ignore -q .env.example` exits non-zero (the file is NOT ignored) and `git check-ignore -q .env.local` exits 0 (it IS ignored).
    - The directory `.next-scaffold` no longer exists.
    - `src/app/layout.tsx` and `src/app/page.tsx` exist; the repo-root `README.md` still contains its original pre-scaffold content (it is rewritten only by plan 01-02).
    - `vitest.config.ts` maps the `@` alias to the `src` directory and sets `test.testTimeout` to at least 30000.
  </acceptance_criteria>
  <done>A fresh `npm install && npm run dev` serves the Next.js app, `npx tsc --noEmit` is clean, Vitest runs, and no secret file can be committed by accident.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the shared airport domain contracts in types.ts</name>
  <files>src/domain/airports/types.ts</files>
  <read_first>
    - `.planning/phases/01-foundation-configuration-airport-registry-resolution/01-RESEARCH.md` — "Registry index shape (Claude's discretion item)" and "Pattern 3: Parallel-runway separation"
    - `.planning/phases/01-foundation-configuration-airport-registry-resolution/01-CONTEXT.md` — D-02 (`ARPT_ID` is the IATA field), D-03, D-06, D-07
    - `.planning/REQUIREMENTS.md` — DATA-01 (per-runway geometry: count, length, parallel-runway separation), RESOLVE-01..04
    - `tsconfig.json` — the `@/*` path mapping produced by Task 2
  </read_first>
  <action>
    Create `src/domain/airports/types.ts` containing every contract that plans 01-03 and 01-04
    build against. Types only — no runtime logic, no imports beyond TypeScript itself. Exports,
    with exact member names:

    `RunwaySummary` — `id: string` (the FAA `RWY_ID`, e.g. `08L/26R`), `lengthFt: number`,
    `widthFt: number`, `surface: string`, `condition: string`, `headingDeg: number` (already
    normalised to the 0–180 half-circle so a strip and its reciprocal share one value),
    `end1: { lat: number; lon: number }`, `end2: { lat: number; lon: number }`.

    `ParallelGroup` — `headingDeg: number`, `runwayIds: string[]`,
    `separationMeters: number | null` (null when the group holds a single runway).

    `AirportRef` — `icao: string` (from the FAA `ICAO_ID` field verbatim; Alaska and Hawaii
    therefore carry `PANC` / `PHNL` natively and are never derived by prefixing `K`),
    `iata: string` (the FAA `ARPT_ID` used directly as the IATA code per D-02),
    `name: string`, `city: string`, `state: string` (2-letter `STATE_CODE`), `lat: number`,
    `lon: number`, `runwayCount: number`, `runways: RunwaySummary[]`,
    `parallelGroups: ParallelGroup[]`.

    `Registry` — `byIcao: ReadonlyMap<string, AirportRef>`,
    `byIata: ReadonlyMap<string, AirportRef>`,
    `byState: ReadonlyMap<string, readonly AirportRef[]>`,
    `all: readonly AirportRef[]`, `fetchedAt: string` (ISO-8601).
    Deliberately no `byRegion` member: region lookup is derived in the resolver from `byState`
    plus the state-to-region table, which keeps the region definitions (D-03/D-04/D-05) inside
    the resolution layer and lets plans 01-03 and 01-04 be written in parallel. Record this as
    the planner's answer to CONTEXT.md's "internal registry data structure" discretion item.

    `RegionName` — a string-literal union of exactly these nine values:
    `'New England'`, `'Mid-Atlantic'`, `'South'`, `'Midwest'`, `'Southwest'`,
    `'Mountain West'`, `'Pacific'`, `'Alaska'`, `'Hawaii'` (D-03).

    `MatchKind` — a string-literal union of exactly:
    `'icao'`, `'iata'`, `'alias'`, `'name'`, `'state'`, `'region'`, `'metro'`, `'none'`.

    `ResolveResult` — `query: string` (the caller's raw input, echoed back),
    `kind: MatchKind`, `matches: readonly AirportRef[]`, `ambiguous: boolean` (true when the
    caller must choose between several equally valid candidates, per D-07/RESOLVE-04),
    `matchedVia: string | null` (a human-readable note explaining a non-obvious match, e.g. a
    legacy-code alias or a metro-cluster expansion; null when the match was direct),
    `suggestions: readonly AirportRef[]` (populated only when `kind` is `'none'`, per D-07 —
    a miss returns an empty result plus nearest candidates, never a thrown exception).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const s=fs.readFileSync('src/domain/airports/types.ts','utf8');const body=s.split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n');const need=['RunwaySummary','ParallelGroup','AirportRef','Registry','RegionName','MatchKind','ResolveResult'];for(const n of need){if(!new RegExp('export (interface|type) '+n+'\\\\b').test(body))throw new Error('missing export: '+n)};for(const f of ['parallelGroups','separationMeters','runwayCount','matchedVia','suggestions','ambiguous','byIcao','byIata','byState','fetchedAt']){if(!body.includes(f))throw new Error('missing member: '+f)};if(/byRegion/.test(body))throw new Error('Registry must not declare byRegion');const Q=String.fromCharCode(39);for(const r of ['New England','Mid-Atlantic','Mountain West','Alaska','Hawaii']){if(!body.includes(Q+r+Q))throw new Error('missing RegionName member: '+r)};console.log('types.ts OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/airports/types.ts` exports all seven of `RunwaySummary`, `ParallelGroup`, `AirportRef`, `Registry`, `RegionName`, `MatchKind`, `ResolveResult`.
    - `AirportRef` declares `icao`, `iata`, `name`, `city`, `state`, `lat`, `lon`, `runwayCount`, `runways`, `parallelGroups`.
    - `ParallelGroup.separationMeters` is typed `number | null`.
    - `Registry` declares `byIcao`, `byIata`, `byState`, `all`, `fetchedAt` and does NOT declare a `byRegion` member.
    - `RegionName` contains exactly nine literal members, including `'New England'`, `'Mid-Atlantic'`, `'Mountain West'`, `'Alaska'`, `'Hawaii'`.
    - `MatchKind` includes `'alias'`, `'metro'`, `'region'`, `'state'` and `'none'`.
    - `ResolveResult` declares `ambiguous`, `matchedVia` and `suggestions`.
    - `npx tsc --noEmit` exits 0; the file contains no `import` of a runtime value and no executable statements.
  </acceptance_criteria>
  <done>Plans 01-03 and 01-04 can both be written against `src/domain/airports/types.ts` without either inventing its own shapes or waiting on the other.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | Third-party code executes during install and at runtime; a typosquat or a compromised release lands inside the app with full process privileges |
| developer working tree → git history | A committed `.env` publishes live credentials permanently |
| server module graph → browser bundle | Any module reachable from a client component ships its contents to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install` of `zod`, `server-only`, `vitest`, and the `create-next-app` package set | high | mitigate | Task 1 is a blocking-human legitimacy gate that runs before any install: verify maintainers, absence of `postinstall`/`preinstall` scripts, and that the generator name is the official `create-next-app@16.3.0` and not a look-alike. Never auto-approvable. |
| T-01-01 | Information Disclosure | `.gitignore` / repository history | high | mitigate | Task 2 keeps the generated `.env*` ignore rule and appends `!.env.example`; the acceptance criteria assert via `git check-ignore` that `.env.local` is ignored and `.env.example` is not, so the negation cannot accidentally un-ignore real secret files. |
| T-01-02 | Information Disclosure | client/server module split | medium | mitigate | Scaffold uses the App Router with a `src/` directory so server-only code lives outside `src/app`; the `server-only` guard package is installed here and applied to the config module in plan 01-02. No `NEXT_PUBLIC_*` variable is introduced anywhere in this phase. |
| T-01-03 | Tampering | `vitest.config.ts` alias resolution | low | accept | A mismatch between the Vitest alias and the tsconfig path mapping causes tests to load a different module than the build does. Accepted because both are asserted to point at the same `src` directory and `npx tsc --noEmit` runs against the tsconfig mapping in every task's verification. |
</threat_model>

<verification>
Run from the repository root after both automated tasks complete:

1. `npm ci` (or `npm install` on first run) — exits 0.
2. `npx tsc --noEmit` — exits 0.
3. `npx vitest run --passWithNoTests` — exits 0.
4. `git check-ignore -q .env.local` exits 0; `git check-ignore -q .env.example` exits non-zero.
5. `git status --porcelain` shows no `node_modules` or `.next` entries.

<human-check>
Start the app with `npm run dev` and open http://localhost:3000. The Next.js starter page
should render without a build error overlay. This is the end-of-phase confirmation that the
scaffold is genuinely runnable, not merely type-clean.
</human-check>
</verification>

<success_criteria>
- A fresh clone plus `npm install` yields a Next.js 16 app that builds, type-checks, lints and boots.
- `npm test` is wired to Vitest with a working `@/` alias and a 30s timeout budget.
- `.env.example` is committable while every other `.env*` file is not.
- `src/domain/airports/types.ts` is the single declaration site for `AirportRef`, `Registry` and `ResolveResult`.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-configuration-airport-registry-resolution/01-01-SUMMARY.md` when done.
Record in it: the exact `create-next-app` invocation that succeeded, the resolved versions of
`next`, `typescript`, `zod`, `server-only` and `vitest`, and the two planner decisions made
here (Vitest as the test runner; `Registry` carries no `byRegion` member).
</output>
