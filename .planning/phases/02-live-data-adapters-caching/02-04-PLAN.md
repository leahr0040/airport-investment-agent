---
phase: 02-live-data-adapters-caching
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - package.json
  - package-lock.json
  - src/domain/adapters/nasStatus.ts
  - src/domain/adapters/nasStatus.test.ts
autonomous: false
requirements: [DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "Asking for an airport's status returns the live FAA delay and closure events currently affecting it."
    - "An airport with nothing wrong returns success with an empty event list, not a failure."
    - "Asking about two different airports issues one upstream fetch, not two."
    - "The FAA feed is matched by the 3-letter FAA location identifier derived from the ICAO code, so West Palm Beach matches on PBI even though its IATA code is now DJT."
    - "A delay block the FAA adds or renames later still yields its airport's events rather than being silently ignored."
    - "An upstream timeout or error returns a typed failure instead of throwing."
  artifacts:
    - "src/domain/adapters/nasStatus.ts — fetchNasStatus, NasStatus, NasStatusEvent, toFaaLid"
    - "src/domain/adapters/nasStatus.test.ts — XML-fixture coverage of every branch"
    - "fast-xml-parser in package.json dependencies, installed only after a blocking human review"
  key_links:
    - "One fixed cache key holds the whole parsed feed — the endpoint takes no query parameters, so a per-airport key would create N independent TTL clocks over one atomic document (RESEARCH.md Pitfall 2)."
    - "The FAA LID is the ICAO code minus its leading country-prefix letter, which is why filtering keys off `icao` rather than `iata` and sidesteps the DJT/PBI rename divergence."
    - "isValidIcao here is a correctness check, not an injection control — the upstream URL is a constant with no variable input (RESEARCH.md Pitfall 3)."
---

<objective>
Build the FAA NAS Status adapter: one keyless whole-feed fetch, XML parsed into JavaScript, cached under a single shared key at a 3-minute TTL (D-01 lru-cache, D-03 per-source TTL), then filtered per airport by FAA location identifier — with a typed failure on every error path (D-06 hard-fail, no stale branch) and a format-checked input (D-08).

Purpose: this is the project's only live signal for what is actually going wrong at an airport right now (ground stops, ground delay programs, arrival/departure delays, closures). Phase 3's unmet-demand reasoning and Phase 4's QUERY-04 answer both need a real, current disruption signal rather than a generic high-utilization claim.

Output: `fetchNasStatus(icao)` returning `AdapterResult<NasStatus>`, unit-tested against XML fixtures with no live network access in the test suite.

This plan is NOT autonomous: it opens with a blocking human checkpoint approving the `fast-xml-parser` dependency, which RESEARCH.md's Package Legitimacy Audit flagged `[SUS]`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-data-adapters-caching/02-CONTEXT.md
@.planning/phases/02-live-data-adapters-caching/02-RESEARCH.md
@.planning/phases/02-live-data-adapters-caching/02-PATTERNS.md
@.planning/phases/02-live-data-adapters-caching/02-01-SUMMARY.md
@.planning/phases/02-live-data-adapters-caching/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve the fast-xml-parser dependency (Package Legitimacy Gate)</name>
  <what-built>Nothing yet. This checkpoint gates the `npm install fast-xml-parser` step in Task 2. RESEARCH.md's Package Legitimacy Audit returned a `[SUS]` verdict for this package, and the Package Legitimacy Gate protocol requires a blocking human review before any `[SUS]` package is installed. This checkpoint is never auto-approvable, regardless of the project's auto-advance setting.</what-built>
  <how-to-verify>
    Reviewer note carried verbatim from `.planning/phases/02-live-data-adapters-caching/02-RESEARCH.md` § Package Legitimacy Audit:

    > `fast-xml-parser` — planner inserts `checkpoint:human-verify` before `npm install fast-xml-parser`. Human reviewer note for that checkpoint: this package has 81.8M weekly downloads and a 9-year-old public GitHub repo; the `SUS` verdict traces specifically to the "too-new" signal firing on the latest-version publish date rather than package age. Recommend approving after a 10-second glance at the repo, but the checkpoint must still occur per the Package Legitimacy Gate protocol — do not silently downgrade the verdict.

    Audit row for reference: registry npm, age ~9 years (created 2017-01-28), 81.8M weekly downloads, source repo `github.com/NaturalIntelligence/fast-xml-parser`, version 5.10.1, no postinstall script (`npm view fast-xml-parser scripts.postinstall` returned empty during research).

    Steps:
    1. Open https://www.npmjs.com/package/fast-xml-parser and confirm the weekly download count is in the tens of millions and the listed repository is `NaturalIntelligence/fast-xml-parser`.
    2. Open that GitHub repository and confirm it is a long-lived project with real commit history, not a days-old repo.
    3. Confirm the package's own dependency list is empty or near-empty on the npm page.
    4. Decide: approve the install, or reject it.

    If you REJECT: the documented alternative from RESEARCH.md is a hand-rolled extractor over the FAA XML. Say so and Task 2 changes to that approach — but note RESEARCH.md's "Don't Hand-Roll" section argues against it, because the FAA `Reason` field is free text that can contain entity-escaped characters and the schema has MEDIUM-confidence blocks that have not been observed live.
  </how-to-verify>
  <read_first>
    - .planning/phases/02-live-data-adapters-caching/02-RESEARCH.md (Package Legitimacy Audit; Standard Stack; Don't Hand-Roll)
    - package.json (current dependency set — confirm no XML parser is present yet)
  </read_first>
  <files>package.json (read-only at this task — no edit until the checkpoint resolves)</files>
  <action>
Halt and present the reviewer note above to the human, verbatim, together with the audit row. Do not install anything, do not create `src/domain/adapters/nasStatus.ts`, and do not proceed to Task 2 until an explicit approval or rejection comes back. This checkpoint is not auto-approvable — the Package Legitimacy Gate protocol applies regardless of the project's auto-advance setting, and the automated verdict must not be silently downgraded on the strength of the download count alone. Record the decision and the human's stated reasoning in `02-04-SUMMARY.md`.
  </action>
  <verify>
    <automated>grep -c "fast-xml-parser" package.json</automated>
  </verify>
  <done>The human has returned an explicit approval or rejection, and that decision is recorded in the summary.</done>
  <acceptance_criteria>
    - The human has explicitly approved or rejected the `fast-xml-parser` install.
    - No `npm install fast-xml-parser` has run before this checkpoint resolved — `grep -c "fast-xml-parser" package.json` returns 0 at the moment this checkpoint is presented.
    - The decision and its rationale are recorded in `02-04-SUMMARY.md`.
  </acceptance_criteria>
  <resume-signal>Type "approved" to install fast-xml-parser, or "rejected" plus your reasoning to switch Task 2 to the hand-rolled extractor.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install fast-xml-parser and implement the whole-feed NAS Status adapter</name>
  <files>package.json, package-lock.json, src/domain/adapters/nasStatus.ts</files>
  <read_first>
    - src/domain/adapters/opensky.ts (the sibling adapter written in plan 02-03 — the `server-only` first line, the try/catch-at-the-boundary shape, the internal error-reason type, and the timeout idiom this module must mirror)
    - src/domain/adapters/cache.ts (withCache, NAS_STATUS_TTL_MS, clearCache)
    - src/domain/adapters/types.ts and src/domain/adapters/validate.ts
    - src/domain/adapters/errors.ts (toAdapterFailure — created in plan 02-01, the shared TimeoutError/reason-passthrough/other-error mapping both adapters call from their catch block)
    - src/domain/airports/regions.ts (the AirportCodes type and the DJT/KPBI entry that motivates the LID derivation)
    - .planning/phases/02-live-data-adapters-caching/02-RESEARCH.md (Code Examples — FAA NAS Status adapter; Architecture Patterns / Pattern 2; Common Pitfalls 2, 3, 5; Open Questions 1)
    - .claude/CLAUDE.md (§ Q1 item 2 — the live-confirmed FAA NAS root schema and the caution to parse defensively because the Ground Delay and Ground Stop blocks were not observed live)
  </read_first>
  <action>
Install the parser, then create `src/domain/adapters/nasStatus.ts`.

1. Run `npm install fast-xml-parser@^5.10.1` — only after Task 1's checkpoint returned an approval. If Task 1 was rejected, skip the install and implement the parsing step with a hand-rolled extractor instead, keeping every other part of this task identical.

2. Module conventions: `import "server-only"` as the first line, mirroring `src/config/env.ts` and the sibling OpenSky adapter. This module holds no credentials, but it sits in the same adapter layer and the guard costs nothing. Emit no log output of any kind — the FAA `Reason` field is upstream free text and must never be printed verbatim (RESEARCH.md Pitfall 5). Do not import anything from `opensky.ts`: RESEARCH.md's anti-pattern list is explicit that the two adapters share only `AdapterResult`, `withCache`, and the validators, never an HTTP client, so a fix to one can never silently change the other.

3. Constant: the feed URL `https://nasstatus.faa.gov/api/airport-status-information` as a module-level string literal. It takes no query parameters at all. Never append one, never interpolate anything into it — a query parameter would be silently ignored by the FAA endpoint, which masks the bug instead of surfacing it (RESEARCH.md Pitfall 2).

4. Parser configuration: construct one module-level `XMLParser` with `parseTagValue` disabled so every extracted value stays a string. Left at its default, a start time such as an FAA clock value would be coerced to a number and lose its leading zero. Also configure it so repeated elements are always arrays for the list-bearing element names — a feed with exactly one delay block or one affected airport otherwise parses to a bare object rather than a one-element array, which is the single most common way this library is misused. If the array configuration option is unavailable, normalise instead with a local `toArray` helper applied at every list boundary; either approach is acceptable so long as the one-element fixture case in Task 3 passes.

5. Exported types:
   - `NasStatusEvent` — `type: string` (the containing `Delay_type` block's `Name` value, e.g. the closures block or a delay-program block), `reason: string | null`, `start: string | null`, `reopen: string | null`, and `raw: Record<string, string>` carrying every other scalar field found on the matched entry. The `raw` bag exists because CLAUDE.md's research could only confirm the closure block's field names live; the ground-stop and ground-delay blocks are MEDIUM confidence, so unknown fields must be preserved rather than dropped.
   - `NasStatus` — `lid: string` (the FAA location identifier used for the match), `icao: string`, `updateTime: string | null` (the feed's `Update_Time`), and `events: NasStatusEvent[]`.

6. Exported helper `toFaaLid(icao: string): string` — returns the ICAO code minus its leading country-prefix character. US ICAO codes are a single prefix letter followed by the 3-letter FAA location identifier, so KATL yields ATL, KPBI yields PBI, PANC yields ANC, PHNL yields HNL. Deriving from ICAO rather than from the `iata` field is deliberate and resolves RESEARCH.md Open Question 1: the FAA feed keys West Palm Beach as PBI while `regions.ts` now carries its IATA code as DJT after the 2026 rename, so an IATA-based match would silently return an empty event list for that airport — a false "nothing wrong here". Add a short comment recording that reasoning and the assumption it rests on. Guard the derived value with `isValidIata` and return reason `invalid_input` if it does not come out as 3 uppercase letters.

7. Exported `fetchNasStatus(icao: string): Promise<AdapterResult<NasStatus>>`:
   - Call `isValidIcao(icao)` first; on false return ok false with reason `invalid_input` before any I/O. Add a comment stating plainly that this check is correctness-only here and not an injection control, because the feed URL is a constant with no user-influenced component — the security narrative for DOC-01 must not claim uniform SSRF coverage across the two adapters (RESEARCH.md Pitfall 3).
   - Wrap the rest in a try/catch that never rethrows.
   - Fetch and parse the whole feed through `withCache` under the single fixed key `nas:feed` at `NAS_STATUS_TTL_MS`, with `AbortSignal.timeout(3000)` and no retry. Read the body as text and parse it. A non-OK status throws an internal error carrying reason `rate_limited` for HTTP 429 and reason `error` otherwise.
   - Filter the parsed document for this airport: walk every `Delay_type` block generically rather than switching on the four known block names, and within each block recursively collect any object carrying an `ARPT` property whose value equals the derived LID. Unknown, added, or renamed block names must therefore still yield their events. Treat a missing block as no active event of that type rather than as a parse failure — CLAUDE.md's research explicitly warns that not all four block types are present at all times.
   - Return ok true with `source` set to `faa-nas-status`, a fresh ISO-8601 `fetchedAt`, and `events` possibly empty. An empty event list is the normal, healthy case and must NOT be reported as `no_data` — "no active delay at this airport" is a real answer that Phase 3 will score.
   - In the catch: call `toAdapterFailure(err, "faa-nas-status")` from `./errors` (plan 02-01) and return its result directly. It already maps a `TimeoutError` name to reason `timeout`, a carried reason to itself, anything else to reason `error`, and enforces the detail-hygiene rule (no response body, header, or credential ever reaches `detail`). Do not re-implement this mapping locally.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').dependencies['fast-xml-parser'])"` prints a version string on the 5.x line (or, if Task 1 was rejected, prints undefined and `nasStatus.ts` contains a hand-rolled extractor).
    - `head -1 src/domain/adapters/nasStatus.ts` is the `server-only` import.
    - The feed URL appears exactly once and is never built from an argument: `grep -c "nasstatus.faa.gov" src/domain/adapters/nasStatus.ts` returns 1, and that line contains no template-literal interpolation.
    - `grep -c "nas:feed" src/domain/adapters/nasStatus.ts` returns 1 — one shared cache key for the whole feed.
    - No logging from this module: `grep -cE "console\.[a-z]+\(" src/domain/adapters/nasStatus.ts` returns 0.
    - `grep -c "AbortSignal.timeout(3000)" src/domain/adapters/nasStatus.ts` returns 1.
    - `grep -c "export function toFaaLid" src/domain/adapters/nasStatus.ts` returns 1.
    - `grep -c "NAS_STATUS_TTL_MS" src/domain/adapters/nasStatus.ts` returns at least 1 — the TTL comes from the shared constant, not a local literal.
    - `grep -c "toAdapterFailure" src/domain/adapters/nasStatus.ts` returns at least 1 — the catch block delegates to the shared helper instead of re-implementing the TimeoutError/reason mapping locally.
    - The module does not import the sibling adapter: `grep -c "from \"./opensky\"" src/domain/adapters/nasStatus.ts` returns 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The adapter fetches and parses the whole feed once per TTL, filters by derived FAA location identifier, and converts every failure mode into a typed result.</done>
</task>

<task type="auto">
  <name>Task 3: Cover the NAS Status adapter against XML fixtures</name>
  <files>src/domain/adapters/nasStatus.test.ts</files>
  <read_first>
    - src/domain/adapters/nasStatus.ts (the implementation written in Task 2)
    - src/domain/adapters/opensky.test.ts (the fetch-stubbing and clearCache idioms established in plan 02-03)
    - src/domain/airports/regions.test.ts (the repo's base test style)
    - .claude/CLAUDE.md (§ Q1 item 2 — the confirmed live root schema: `AIRPORT_STATUS_INFORMATION` containing `Update_Time` and `Delay_type` blocks, each with a `Name` and, for closures, an `Airport_Closure_List` of `Airport` entries carrying `ARPT`, `Reason`, `Start`, `Reopen`)
    - .planning/phases/02-live-data-adapters-caching/02-RESEARCH.md (Pattern 2; Pitfalls 2 and 3; Open Questions 1)
  </read_first>
  <action>
Write `src/domain/adapters/nasStatus.test.ts` covering the adapter's full contract. Stub the network with `vi.stubGlobal("fetch", vi.fn())` and call `clearCache()` in `beforeEach`; restore stubs in `afterEach`. No test in this file may touch the live network.

Declare XML fixture strings as module-level constants in the test file, shaped after the confirmed live schema: an `AIRPORT_STATUS_INFORMATION` root containing an `Update_Time` element and one or more `Delay_type` blocks, each with a `Name` and a nested list of `Airport` entries carrying `ARPT`, `Reason`, `Start`, and `Reopen`.

Cases:
1. Malformed input — `fetchNasStatus("katl")` and `fetchNasStatus("ATL")` each resolve to ok false with reason `invalid_input`, and the fetch mock was never called.
2. Happy path — a fixture whose closure block lists ATL returns ok true for `KATL`, with `source` equal to `faa-nas-status`, `lid` equal to ATL, `updateTime` equal to the fixture's value, and exactly one event whose reason, start, and reopen match the fixture strings.
3. Healthy airport — the same fixture queried for `KORD` (absent from the feed) returns ok true with an empty `events` array. Assert explicitly that `ok` is true; "no active delay" is a real answer and must never surface as a failure.
4. Single-element list — a fixture with exactly one `Delay_type` block containing exactly one `Airport` entry still yields one event. This is the case the parser's array handling exists for; without it the parsed shape is a bare object and the walk finds nothing.
5. Multiple blocks — a fixture with two differently-named `Delay_type` blocks, one listing the queried airport and one not, yields exactly one event whose `type` is the name of the block that listed it.
6. Unknown block name — a fixture containing a `Delay_type` block with a name not mentioned anywhere in the implementation, nesting an entry whose `ARPT` matches, still yields that event. This proves the generic walk rather than a hardcoded switch over the four documented block types.
7. FAA LID divergence — `toFaaLid("KPBI")` is PBI, and `fetchNasStatus("KPBI")` against a fixture whose `ARPT` is PBI returns one event. Add a comment in the test naming why: `regions.ts` carries West Palm Beach's IATA code as DJT after the rename, so an IATA-keyed match would return a false empty here.
8. Prefix derivation for the non-K codes — `toFaaLid("PANC")` is ANC and `toFaaLid("PHNL")` is HNL, and a fixture listing ANC matches a `PANC` query.
9. Shared cache key — two calls for two different airports (`KATL` then `KORD`) against the same stubbed response issue exactly one fetch. This is DATA-04 asserted for the whole-feed source and is the direct counter-proof to Pitfall 2's per-airport-key mistake.
10. Cache expiry — with fake timers, advance past the NAS TTL and assert a second fetch occurs.
11. Timeout, rate limit, and server error, table-driven — using `it.each`, assert (a) a fetch rejecting with an error whose `name` is `TimeoutError` produces reason `timeout`, (b) HTTP 429 produces reason `rate_limited`, and (c) HTTP 503 produces reason `error`. All three rows exercise the shared `toAdapterFailure` helper from plan 02-01 (already unit-tested there for the mapping and the detail-hygiene rule) — this table only needs to prove the adapter wires its own caught/thrown errors into that helper correctly.
12. Detail hygiene — in the 503 case, assert the returned `detail` does not contain the fixture's `Reason` free text. Upstream free text must not ride out on the failure path.
13. Free-text passthrough — a fixture whose `Reason` contains an entity-escaped ampersand and an angle bracket parses without throwing, and the resulting `reason` value is a plain string. This is the entity-handling edge case that motivates using a real parser rather than a regex extractor.
  </action>
  <verify>
    <automated>npx vitest run src/domain/adapters/nasStatus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/adapters/nasStatus.test.ts` exits 0 with at least 12 `it(`/`it.each(` blocks covering every numbered case (case 11's timeout/rate-limit/server-error trio is one `it.each` block covering three rows): `grep -cE "it\(|it\.each\(" src/domain/adapters/nasStatus.test.ts` returns at least 12.
    - `grep -c "toHaveBeenCalledTimes" src/domain/adapters/nasStatus.test.ts` returns at least 4 — the invalid-input, shared-cache-key, cache-expiry, and one error case all assert call counts.
    - `grep -c "KPBI" src/domain/adapters/nasStatus.test.ts` returns at least 1.
    - `grep -c "stubGlobal" src/domain/adapters/nasStatus.test.ts` returns at least 1 — no test reaches the live network.
    - `npm test` exits 0 across the whole suite, `npx tsc --noEmit` exits 0, `npm run lint` exits 0.
  </acceptance_criteria>
  <done>Every branch of the NAS adapter is covered by fixture-driven tests, including the single-element parse case, the unknown-block case, and the DJT/PBI divergence.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Kind | File |
|---|---|---|
| `fetchNasStatus` | async function | `src/domain/adapters/nasStatus.ts` |
| `toFaaLid` | function | `src/domain/adapters/nasStatus.ts` |
| `NasStatus` | exported type | `src/domain/adapters/nasStatus.ts` |
| `NasStatusEvent` | exported type | `src/domain/adapters/nasStatus.ts` |
| `fast-xml-parser` | new npm dependency (gated by a blocking human checkpoint) | `package.json` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `fetchNasStatus(icao)` | The airport code is untrusted analyst-derived text, but unlike the OpenSky adapter it never reaches an outbound URL — it only selects rows from an already-fetched document. |
| adapter → FAA NAS Status API | Keyless server-to-server HTTPS call to a fixed, parameter-less URL. |
| FAA XML → app memory | Upstream free text (the `Reason` field) enters the system here for the first time and flows onward to Phase 3 and eventually into LLM context in Phase 4. |
| npm registry → build | A new third-party dependency enters the supply chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | `npm install fast-xml-parser` — `[SUS]` legitimacy verdict | high | mitigate | Task 1 is a blocking human checkpoint, never auto-approvable, carrying the audit's reviewer note verbatim (81.8M weekly downloads, 9-year-old repo, verdict traced to a "too-new" heuristic reading the latest version's publish date). Version pinned to the registry-verified 5.10.1 line; research confirmed no postinstall script. A rejection path to a hand-rolled extractor is specified so the checkpoint is a real decision, not a rubber stamp. |
| T-02-11 | Tampering | FAA `Reason` free text staged for prompt injection in Phase 4 | medium | transfer | Sanitising for LLM context is SEC-04's job in Phase 4. Mitigated here by keeping `reason` a plain typed string on `NasStatusEvent` — never rendered as markup, never concatenated into a template — so Phase 4 has one isolated field to sanitise and truncate. |
| T-02-02 | Information Disclosure | `detail` on the failure branch, module logging | high | mitigate | No log statements at all (asserted by grep); the catch block delegates to the shared `toAdapterFailure` helper (plan 02-01, `src/domain/adapters/errors.ts`), which enforces the detail-hygiene rule in one place; this adapter's own test suite still asserts the fixture's free-text reason never appears in `detail`. |
| T-02-12 | Spoofing | A LID/IATA mismatch making a disrupted airport look healthy | medium | mitigate | The LID is derived from the ICAO code rather than the IATA code, which is exactly the DJT/PBI case that would otherwise produce a silent false empty; asserted directly in the test suite. The derived value is re-checked with `isValidIata` before use, so a malformed derivation returns `invalid_input` instead of an empty match. |
| T-02-05 | Denial of Service | a hung FAA connection stalling the whole answer | medium | mitigate | `AbortSignal.timeout(3000)` with no retry (D-07); the failure surfaces as a typed `timeout` and only this source's KPIs degrade. |
| T-02-13 | Denial of Service | XML entity expansion / oversized upstream document | low | accept | The feed is a small, fixed, government-operated document over HTTPS; `fast-xml-parser` does not resolve external entities by default, and the 3-second timeout bounds the read. Revisit only if the source is ever swapped for an untrusted publisher. |
| T-02-03 | Information Disclosure | module reachable from a client bundle | medium | mitigate | `import "server-only"` as the first line, matching the sibling adapter, so this upstream endpoint is never exposed to the browser (supports SEC-01 in Phase 5). |
</threat_model>

<verification>
- `npm test` exits 0 across the whole suite with no live network access from any test.
- `npx tsc --noEmit` and `npm run lint` exit 0.
- Task 1's checkpoint decision is recorded in the summary.
- The diff touches exactly four files.
</verification>

<success_criteria>
- DATA-03: `fetchNasStatus` returns the live FAA delay and closure events affecting a named airport, and returns success with an empty list when there are none.
- DATA-04: two different airports queried inside the TTL cost one upstream fetch, proven by an asserted call count.
- DATA-05: timeout, rate-limit, server-error, and malformed-input paths all return a typed failure; nothing throws past the adapter boundary.
- The `[SUS]` dependency entered the project only through an explicit, recorded human approval.
</success_criteria>

<output>
Create `.planning/phases/02-live-data-adapters-caching/02-04-SUMMARY.md` when done.
</output>
